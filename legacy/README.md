# Material previo y legado (no usado por la SPA Vite)

Esta carpeta agrupa **HTML/JS/CSS antiguos**, clones de referencia, guías **M5/M6** y archivos auxiliares para no saturar la raíz del repositorio en GitHub.

La aplicación entregable es la **SPA Vue** en `src/` en la raíz del repo.
